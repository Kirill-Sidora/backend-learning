export const ApplicationDefault = {
    SERVICE_NAME: 'backend-learning',
    DEFAULT_PORT: 3000,
    DEFAULT_ID: '127.0.0.1',
} as const;

export const Paths = {
    DOCS_PATH: '/docs',
} as const;

export const TYPE_UNDEFINED = 'undefined' as const;